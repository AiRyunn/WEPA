name: diversity
common:
  seed: 42
  device: "0"
  dataset_size: 1
  prompt_candidate_pool_size: 100
  max_length: 10
  samples_per_prompt: 100
  k_values: [0, 1, 2, 3]
  output_dir: artifacts/output/diversity
parameters:
- model_name: meta-llama/Llama-3.2-3B
